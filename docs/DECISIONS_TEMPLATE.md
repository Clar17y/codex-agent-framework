# Decisions record

- Checkout identity: `<repository>@<worktree or checkout path>`
- Author: `<name or agent>`
- Revision: `<candidate HEAD>`
- Source paths: `<paths inspected or changed>`
- Invalidation: `<scope, revision, or dependency change that makes this stale>`

## Decisions

- `<decision>` — `<reason and affected paths>`

Keep credentials out. Reconstruct this record if scratch cleanup sweeps it.
