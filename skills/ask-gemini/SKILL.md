---
name: ask-gemini
description: Delegate routine implementation or explicitly requested Gemini work to the installed agy CLI, falling back to DeepSeek Flash v4.1 upon Gemini quota exhaustion, and native Luna medium as the final fallback.
---

Use `{{CODEX_ROOT}}/agent-framework/scripts/provider_runner.py` with Python 3.11 or newer (`python3` on macOS; `python` on Windows). Read `{{CODEX_ROOT}}/agent-framework/docs/FRAMEWORK.md` for the contract and command. Invoke role `implement` from the explicitly authorized active workspace/worktree. The configured primary model is exactly `gemini-3.8-flash-medium`.

Before preparing or launching a provider task, run `python "{{CODEX_ROOT}}/agent-framework/scripts/provider_runner.py" status --provider gemini --config "{{CODEX_ROOT}}/agent-framework/routing.json"` (use `python3` on macOS). This local check needs no task/workspace and never contacts Gemini. On exit 20 / `fallback_required`, inspect the structured result: if DeepSeek Flash v4.1 is configured, it falls back to its local `deepseek-flash` alias; otherwise it falls back directly to native Luna medium. DeepSeek runs via `codex exec -p deepseek --model deepseek-flash` and performs a live, fail-closed monetary-balance preflight (`GET /user/balance` using `DEEPSEEK_API_KEY`). A depleted balance or preflight HTTP 402 exits 20 before model launch; a missing key or unverified preflight exits 1. Both route to native Luna medium without launching the model. A runtime HTTP 402 can occur only after launch and also routes to Luna. On `state_error`, disclose the state-read failure and use the Luna fallback; preserve state for investigation. Only `available_to_try` permits a provider attempt; it is not a live usage balance. Use the installed config for every workspace, so `state/gemini-quota.json` and `state/deepseek-balance.json` are shared. A custom `--state-dir` is for deliberately separate state, not a way to retry an exhausted account. Preflight checks quota only; confirm no active or uncertain writer owns the assigned files before starting any fallback.

The normal `implement` command enforces the cache too. Confirmed exhaustion records the observation, evidence, and next eligible attempt. A known reset is recorded separately from a probe cooldown; after expiry, the next task may try Gemini again. No background monitor or repeated provider polling is needed. To record exhaustion explicitly reported by the user, use `quota-set --provider gemini --config "{{CODEX_ROOT}}/agent-framework/routing.json" --reason "User-reported exhaustion"`, optionally with `--reset-at` containing a future ISO timestamp and timezone. Without a known reset, the configured cooldown applies. Never invent a reset or use this command to disguise another failure.

Provide compact, fresh-context task contracts with concrete acceptance criteria, explicit non-overlapping owned paths, validation commands, and applicable instructions. Avoid tiny fragmented assignments and avoid pasting conversational history or raw log dumps into the contract. The delegated worker (Gemini, DeepSeek Flash, or native Luna fallback) owns the complete bounded execution loop within those paths: local investigation, implementation, focused validation, routine repairs, waiting for commands/processes and interpreting results, and self-review until criteria pass or a concrete blocker is escalated. Do not invoke it in a canonical checkout when repository policy requires a provisioned worktree. Scope and write permissions must be supported by the user's task. The user explicitly authorized the adapter's CLI bypass mode. Codex sandbox and automatic approval review still apply.

Use this route when the primary chooses delegation; small, well-understood fixes can be completed directly without invoking this skill. The primary concentrates on scope, architecture, ownership, material decisions, integration, risk assessment, and final acceptance. When repairs are needed after review or verification, return them to the existing worker who owns the files and context when practical. Workers give concise handoffs; separate process records and additional gates are optional unless explicitly required.

Read the adapter's structured result and evidence files. On exit 20 / `fallback_required`, inspect the recommended fallback: if DeepSeek Flash is recommended, execute the implementation task with DeepSeek Flash (`--provider deepseek`); if DeepSeek Flash is exhausted/depleted or fails preflight, immediately delegate the same contract and partial changes to native `implementer`, explicitly `gpt-5.6-luna` / `medium`; no additional user approval is needed for the already authorized task. Preserve fallback continuity by supplying the original contract, existing changes, prior output, and unfinished checks. Keep all remaining routine work on this route while the quota cache is active. Only one writer may own the files, and previous providers must have exited before the next starts. The replacement must inspect partial edits before continuing.

Cached quota expires at a known reset or a bounded probe time; a probe is not a claim that quota reset. If the checked Gemini route is unavailable or blocked (including `blocked_pending_run`, authentication, model, launch, timeout, or provider permission failures), report the actual reason and route non-quota failures directly to native `implementer`, `gpt-5.6-luna` / `medium`, without asking the user again. Do not misclassify these failures as quota exhaustion or clear unresolved pending state. Confirm any prior writer has stopped before handing over overlapping files; a pending run in another workspace does not block a fresh Luna task with non-overlapping ownership. Codex permissions and safety review still apply. Escalate hard implementation defects through the complex-implementation route. Worker claims alone are not test evidence; assess the candidate and its check evidence, reusing credible matching evidence and rerunning checks only when relevant inputs change, evidence is missing or unreliable, or a specific concern warrants it.

## Scope confirmation

Include this guidance in delegated implementation contracts and preserve it in fallback handoffs:

Before implementation, confirm that the assigned change addresses the underlying cause or intended feature behavior and relevant affected cases. Look for related uses and existing reusable components, keeping investigation proportional. If the assignment misses coverage or a better shared implementation would cross your owned files, tell the primary before expanding ownership; do not silently deliver a known-incomplete fix. Validate representative affected cases and mention deliberate exclusions concisely. This is a lightweight scope check, not a required report or extra review round.

## Implementation handoff

Include this requirement explicitly in the task contract and preserve it in any Luna fallback handoff:

Before handing over implementation, inspect your changes for reuse, clarity, and unnecessary work. Apply justified simplifications within your ownership, preserve observable behavior and others' edits, and rerun affected checks if needed. Perform this pass yourself; separate review or verification is chosen by the primary according to risk. Return a concise handoff with completion or blocker status, changed paths within assigned ownership, key design decisions, candidate identity (e.g. HEAD plus scoped diff/status), exact checks and results, residual concerns, and useful log locations, without status chatter or raw command dumps.

## Concurrent runs

Gemini tasks may run concurrently in separate worktrees or in the same workspace with disjoint `owned_paths`. Declare literal files or directories in each contract, without glob patterns; a directory includes its descendants, and empty ownership conservatively claims the entire workspace. Overlapping active or uncertain claims block the conflicting task; unrelated files remain available. The adapter holds a short lock only while coordinating claims. Use the result's `pending_path` to locate unresolved evidence. Account quota remains shared. Legacy pending records are preserved and scoped by their recorded workspace or log path; records without file ownership protect their entire workspace, and unknown workspace ownership remains blocked pending investigation. Claude reviews run concurrently without these Gemini gates. Do not run an old adapter copy concurrently with the updated adapter.
